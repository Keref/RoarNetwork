/*
 * Footer Messages
 *
 * This contains all the text for the Footer component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.MessageBox';

export default defineMessages({
  author: {
    id: `${scope}.author`,
    defaultMessage: 'Author.',
  },
});
