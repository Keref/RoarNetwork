/**
 * ActionIcons link to a certain page, an anchor tag
 */

import styled from 'styled-components';

const ActionIcons = styled.a`
  padding: 2px;
  border-radius: 4px;

  &:hover {
	background-color: #f2f2f2;
  }
`;

export default ActionIcons;
